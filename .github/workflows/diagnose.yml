name: Diagnose selectors

# Manual only. Prints a structural summary of a chain's live pages to the log so
# selectors can be written from real markup rather than guessed. Writes nothing.
on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'dom = rendered page structure, api = ordering endpoints'
        type: choice
        options: [dom, api, network]
        default: network
      chain:
        description: 'Chain to diagnose (dom/network modes)'
        type: choice
        options: [dominos, papa_johns, pizza_hut]
        default: papa_johns

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Diagnose rendered DOM
        if: inputs.mode == 'dom'
        run: npx tsx scrapers/diagnose.ts ${{ inputs.chain }}

      - name: Diagnose ordering endpoints
        if: inputs.mode == 'api'
        run: npx tsx scrapers/diagnose-api.ts

      - name: Diagnose network calls
        if: inputs.mode == 'network'
        run: npx tsx scrapers/diagnose-network.ts ${{ inputs.chain }}
